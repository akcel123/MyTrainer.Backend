{
  "Logging": {
    "Debug": { "LogLevel": { "Default": "Trace" } }, //Для режима дебаг устанавливаем по умолчанию уровень логгирования Information (это что то другое)

    "Console": {
      "LogLevel": {
        "Default": "Trace",
        "Microsoft.AspNetCore": "Information"
      }
    }, //Для консоли устанавливаем логгирование уровня Warning
    "LogLevel": { 
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    } //Я так понял, это общее правило для всех провайдеров (в нашем случае консоль и отладка)
  },

  "AllowedHosts": "*",
  "ConnectionStrings": {
    "training_db": "Server=192.168.0.10;Port=5432;Database=Trainings;User Id=postgres;Password=561749;"
  }
}
